/* Hero Section */
.hero {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    text-align: center;
    padding: 5rem;
}

.hero-text h1 {
    font-size: 4.5rem;
    color: #222;
    font-weight: 700;
}

.hero-text h1 span {
    color: #FFAE00;
}

.hero-text h3 {
    font-size: 2.5rem;
    font-weight: 500;
    color: #555;
}

.hero-text p {
    font-size: 1.6rem;
    color: #777;
    margin-top: 1rem;
}

/* Buttons */
.btn button {
    background: #FFAE00;
    color: white;
    border: none;
    padding: 1rem 2.5rem;
    font-size: 1.8rem;
    border-radius: 50px;
    cursor: pointer;
    box-shadow: 0 5px 10px rgba(255, 174, 0, 0.4);
}

.btn button:hover {
    background: #222;
    color: #FFAE00;
    transform: scale(1.1);
}